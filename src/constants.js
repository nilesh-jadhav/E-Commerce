const API_VERSION = 'v2';
const API_URL = `https://yts.am/api/${API_VERSION}`;

const constants = {
  MOVIES_URL: `${API_URL}/list_movies.json`,
  MOVIE_URL: `${API_URL}/movie_details.json`
};

export default constants;
